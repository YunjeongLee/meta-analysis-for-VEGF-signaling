label = list(tnf ~ "(TNF-α) levels")
style = list(tnf ~ style_number(format = "log10"))
perio1class %>% select(condition, tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(tnf ~ "(TNF-α) levels"),
style = list(tnf ~ style_number(format = "log10"))
) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)",
"Min - Max", "Skewness"))
perio1class %>% select(condition, tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(tnf ~ "(TNF-α) levels"),
style = list(tnf ~ style_number(format = "log10"))
) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)",
"Min - Max", "Skewness"))
perio1class %>% select(condition, tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**")
perio1class %>% select(condition, log10tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(log10tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min - Max", "Skewness"))
perio1class$log10tnf <- log10(perio1class$tnf)
perio1class %>% select(condition, log10tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(log10tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min - Max", "Skewness"))
t-test(tnf ~ condition, data=perio1class)
t.test(tnf ~ condition, data=perio1class)
t.test(tnf ~ condition, data=log10tnf)
t.test(tnf ~ condition, data=log10tnf)
perio1class$log10tnf <- log10(perio1class$tnf)
perio1class$log10tnf <- log10(perio1class$tnf)
# The R chunk use option include = TRUE to exclude this R code/chunk
# in the output file.
# I use the following line 15 to avoid comments within R chunks wrapping
# around to the next line when I create a Word file.
########10########20########30########40########50########60########70#######7
# Global options, echo = TRUE to include R code/chunk in the output file
# fig.height and fig.width defines the default size in inches for figures
knitr::opts_chunk$set(echo = TRUE, comment = "",
fig.height = 3.5, fig.width = 6.5)
# Load libraries
library(e1071) # Needed for skewness()
library(tidyverse) # Need to summarize the data and for %>%
library(flextable) # Used to create a nice summary table in Word
library(gtsummary) # Used to create a nice summary table in Word
library(ggplot2) # Used to create figures using ggplot2
# I typically hide the R code that loads libraries.  NOTE: If you do not use
# the option include=FALSE, this code may not knitr as a pdf file.  The
# message produced when loading tidyverse can cause problems.
perio1class <- read.csv(file = "C:/Users/lione/Desktop/UCONJ510_Introductory_Laboratory_Based_Biostatistics/HW/HW2/perio1class.csv")
perio1class %>% select(condition, tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**")
perio1class$log10tnf <- log10(perio1class$tnf)
perio1class %>% select(condition, log10tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(log10tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min - Max", "Skewness"))
t.test(tnf ~ condition, data=log10tnf)
perio1class %>% select(condition, log10tnf) %>%
t.test(tnf ~ condition, data=log10tnf)
perio1class$log10tnf <- log10(perio1class$tnf)
perio1class %>% select(condition, log10tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(log10tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min - Max", "Skewness"))
t.test(log10tnf ~ condition, data=perio1class)
t.test(log10tnf~condition, data=perio1class, alternative="two.sided",
paired=FALSE, var.equal=TRUE, conf.level=0.95)
# Enter the data for IL-6 for the Vehicle as a vector, V_IL6
V_IL6 <-c(11.8, 80.6, 97.7, 29.2, 32.1, 82.4, 21.6, 124.6)
# Original data shown below with greater precision
# V_IL6 <-c(11.7536, 80.57252, 97.68274, 29.17671, 32.13475, 82.44733,
# 21.64176, 124.6121)
# Enter data for IL-6 for D+Q as a vector, DandQ_IL6
DandQ_IL6 <- c(0.3, 6.4, 32.4, 23.8, 5.4, 1.9, 1.3, 21.3)
# Original data shown below with greater precision
# DandQ_IL6 <- c(0.276294, 6.369392, 32.38206, 23.75031, 5.364931,
# 1.869404, 1.27524, 21.29226)
# Create variable to indicate the group. Both a numerical variable (group),
# coded 1 for Vehicle and 2 for D+Q, and a factor variable (group.f) with
# descriptive labels.
group <- c(rep(1,8), rep(2,8))
group.f <- factor(group, levels=c(1,2), labels=c("Vehicle", "D+Q"))
# Combine the data into a tibble. A tibble has a similar structure to a
# data frame. Each individual (biological replicate) is entered on a
# separate row.
xudata <- tibble(group.f=group.f, group=group, IL6=c(V_IL6, DandQ_IL6))
xudata %>% select(group.f, IL6) %>%
tbl_summary(by = group.f,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(IL6 ~ "IL 6")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min, Max", "Skewness"))
tibble(difference = V_IL6 - DandQ_IL6) %>%
tbl_summary(missing = "ifany",
type = c(all_continuous() ~ "continuous2",
difference ~ "continuous2"),
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(difference ~ "Vehicle - D+Q, IL 6")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)",
"Min, Max", "Skewness"))
t.test(IL6 ~ group.f, data=xudata, paired=TRUE)
# The R chunk use option include = TRUE to exclude this R code/chunk
# in the output file.
# I use the following line 15 to avoid comments within R chunks wrapping
# around to the next line when I create a Word file.
########10########20########30########40########50########60########70#######7
# Global options, echo = TRUE to include R code/chunk in the output file
# fig.height and fig.width defines the default size in inches for figures
knitr::opts_chunk$set(echo = TRUE, comment = "",
fig.height = 3.5, fig.width = 6.5)
# Load libraries
library(e1071) # Needed for skewness()
library(tidyverse) # Need to summarize the data and for %>%
library(flextable) # Used to create a nice summary table in Word
library(gtsummary) # Used to create a nice summary table in Word
library(ggplot2) # Used to create figures using ggplot2
# I typically hide the R code that loads libraries.  NOTE: If you do not use
# the option include=FALSE, this code may not knitr as a pdf file.  The
# message produced when loading tidyverse can cause problems.
perio1class <- read.csv(file = "C:/Users/lione/Desktop/UCONJ510_Introductory_Laboratory_Based_Biostatistics/HW/HW2/perio1class.csv")
perio1class %>% select(condition, tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**")
perio1class$log10tnf <- log10(perio1class$tnf)
perio1class %>% select(condition, log10tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(log10tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min - Max", "Skewness"))
t.test(log10tnf~condition, data=perio1class, alternative="two.sided",
paired=FALSE, var.equal=TRUE, conf.level=0.95)
# Enter the data for IL-6 for the Vehicle as a vector, V_IL6
V_IL6 <-c(11.8, 80.6, 97.7, 29.2, 32.1, 82.4, 21.6, 124.6)
# Original data shown below with greater precision
# V_IL6 <-c(11.7536, 80.57252, 97.68274, 29.17671, 32.13475, 82.44733,
# 21.64176, 124.6121)
# Enter data for IL-6 for D+Q as a vector, DandQ_IL6
DandQ_IL6 <- c(0.3, 6.4, 32.4, 23.8, 5.4, 1.9, 1.3, 21.3)
# Original data shown below with greater precision
# DandQ_IL6 <- c(0.276294, 6.369392, 32.38206, 23.75031, 5.364931,
# 1.869404, 1.27524, 21.29226)
# Create variable to indicate the group. Both a numerical variable (group),
# coded 1 for Vehicle and 2 for D+Q, and a factor variable (group.f) with
# descriptive labels.
group <- c(rep(1,8), rep(2,8))
group.f <- factor(group, levels=c(1,2), labels=c("Vehicle", "D+Q"))
# Combine the data into a tibble. A tibble has a similar structure to a
# data frame. Each individual (biological replicate) is entered on a
# separate row.
xudata <- tibble(group.f=group.f, group=group, IL6=c(V_IL6, DandQ_IL6))
xudata %>% select(group.f, IL6) %>%
tbl_summary(by = group.f,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(IL6 ~ "IL 6")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min, Max", "Skewness"))
tibble(difference = V_IL6 - DandQ_IL6) %>%
tbl_summary(missing = "ifany",
type = c(all_continuous() ~ "continuous2",
difference ~ "continuous2"),
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(difference ~ "Vehicle - D+Q, IL 6")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)",
"Min, Max", "Skewness"))
t.test(IL6 ~ group.f, data=xudata, paired=TRUE)
```{r numerical summaries log base 10}
# The R chunk use option include = TRUE to exclude this R code/chunk
# in the output file.
# I use the following line 15 to avoid comments within R chunks wrapping
# around to the next line when I create a Word file.
########10########20########30########40########50########60########70#######7
# Global options, echo = TRUE to include R code/chunk in the output file
# fig.height and fig.width defines the default size in inches for figures
knitr::opts_chunk$set(echo = TRUE, comment = "",
fig.height = 3.5, fig.width = 6.5)
# Load libraries
library(e1071) # Needed for skewness()
library(tidyverse) # Need to summarize the data and for %>%
library(flextable) # Used to create a nice summary table in Word
library(gtsummary) # Used to create a nice summary table in Word
library(ggplot2) # Used to create figures using ggplot2
# I typically hide the R code that loads libraries.  NOTE: If you do not use
# the option include=FALSE, this code may not knitr as a pdf file.  The
# message produced when loading tidyverse can cause problems.
perio1class <- read.csv(file = "C:/Users/lione/Desktop/UCONJ510_Introductory_Laboratory_Based_Biostatistics/HW/HW2/perio1class.csv")
perio1class %>% select(condition, tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**")
perio1class$log10tnf <- log10(perio1class$tnf)
perio1class %>% select(condition, log10tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(log10tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min - Max", "Skewness"))
t.test(log10tnf~condition, data=perio1class, alternative="two.sided",
paired=FALSE, var.equal=TRUE, conf.level=0.95)
# Enter the data for IL-6 for the Vehicle as a vector, V_IL6
V_IL6 <-c(11.8, 80.6, 97.7, 29.2, 32.1, 82.4, 21.6, 124.6)
# Original data shown below with greater precision
# V_IL6 <-c(11.7536, 80.57252, 97.68274, 29.17671, 32.13475, 82.44733,
# 21.64176, 124.6121)
# Enter data for IL-6 for D+Q as a vector, DandQ_IL6
DandQ_IL6 <- c(0.3, 6.4, 32.4, 23.8, 5.4, 1.9, 1.3, 21.3)
# Original data shown below with greater precision
# DandQ_IL6 <- c(0.276294, 6.369392, 32.38206, 23.75031, 5.364931,
# 1.869404, 1.27524, 21.29226)
# Create variable to indicate the group. Both a numerical variable (group),
# coded 1 for Vehicle and 2 for D+Q, and a factor variable (group.f) with
# descriptive labels.
group <- c(rep(1,8), rep(2,8))
group.f <- factor(group, levels=c(1,2), labels=c("Vehicle", "D+Q"))
# Combine the data into a tibble. A tibble has a similar structure to a
# data frame. Each individual (biological replicate) is entered on a
# separate row.
xudata <- tibble(group.f=group.f, group=group, IL6=c(V_IL6, DandQ_IL6))
xudata %>% select(group.f, IL6) %>%
tbl_summary(by = group.f,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(IL6 ~ "IL 6")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min, Max", "Skewness"))
tibble(difference = V_IL6 - DandQ_IL6) %>%
tbl_summary(missing = "ifany",
type = c(all_continuous() ~ "continuous2",
difference ~ "continuous2"),
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(difference ~ "Vehicle - D+Q, IL 6")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)",
"Min, Max", "Skewness"))
t.test(IL6 ~ group.f, data=xudata, paired=TRUE)
# The R chunk use option include = TRUE to exclude this R code/chunk
# in the output file.
# I use the following line 15 to avoid comments within R chunks wrapping
# around to the next line when I create a Word file.
########10########20########30########40########50########60########70#######7
# Global options, echo = TRUE to include R code/chunk in the output file
# fig.height and fig.width defines the default size in inches for figures
knitr::opts_chunk$set(echo = TRUE, comment = "",
fig.height = 3.5, fig.width = 6.5)
# Load libraries
library(e1071) # Needed for skewness()
library(tidyverse) # Need to summarize the data and for %>%
library(flextable) # Used to create a nice summary table in Word
library(gtsummary) # Used to create a nice summary table in Word
library(ggplot2) # Used to create figures using ggplot2
# I typically hide the R code that loads libraries.  NOTE: If you do not use
# the option include=FALSE, this code may not knitr as a pdf file.  The
# message produced when loading tidyverse can cause problems.
perio1class <- read.csv(file = "C:/Users/lione/Desktop/UCONJ510_Introductory_Laboratory_Based_Biostatistics/HW/HW2/perio1class.csv")
perio1class %>% select(condition, tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**")
perio1class$log10tnf <- log10(perio1class$tnf)
perio1class %>% select(condition, log10tnf) %>%
tbl_summary(by = condition,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(log10tnf ~ "(TNF-α) levels")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min - Max", "Skewness"))
t.test(log10tnf~condition, data=perio1class, alternative="two.sided",
paired=FALSE, var.equal=TRUE, conf.level=0.95)
# Enter the data for IL-6 for the Vehicle as a vector, V_IL6
V_IL6 <-c(11.8, 80.6, 97.7, 29.2, 32.1, 82.4, 21.6, 124.6)
# Original data shown below with greater precision
# V_IL6 <-c(11.7536, 80.57252, 97.68274, 29.17671, 32.13475, 82.44733,
# 21.64176, 124.6121)
# Enter data for IL-6 for D+Q as a vector, DandQ_IL6
DandQ_IL6 <- c(0.3, 6.4, 32.4, 23.8, 5.4, 1.9, 1.3, 21.3)
# Original data shown below with greater precision
# DandQ_IL6 <- c(0.276294, 6.369392, 32.38206, 23.75031, 5.364931,
# 1.869404, 1.27524, 21.29226)
# Create variable to indicate the group. Both a numerical variable (group),
# coded 1 for Vehicle and 2 for D+Q, and a factor variable (group.f) with
# descriptive labels.
group <- c(rep(1,8), rep(2,8))
group.f <- factor(group, levels=c(1,2), labels=c("Vehicle", "D+Q"))
# Combine the data into a tibble. A tibble has a similar structure to a
# data frame. Each individual (biological replicate) is entered on a
# separate row.
xudata <- tibble(group.f=group.f, group=group, IL6=c(V_IL6, DandQ_IL6))
xudata %>% select(group.f, IL6) %>%
tbl_summary(by = group.f,
missing = "ifany",
type = all_continuous() ~ "continuous2",
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(IL6 ~ "IL 6")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)","Min, Max", "Skewness"))
tibble(difference = V_IL6 - DandQ_IL6) %>%
tbl_summary(missing = "ifany",
type = c(all_continuous() ~ "continuous2",
difference ~ "continuous2"),
statistic = all_continuous() ~ c("{mean} ({sd})",
"{median} ({p25}, {p75})",
"{min}, {max}",
"{skewness}"),
digits = list(all_continuous() ~ 1),
label = list(difference ~ "Vehicle - D+Q, IL 6")) %>%
modify_header(label = "**Variable**") %>%
add_stat_label(label = all_continuous() ~ c("Mean (SD)", "Median (IQR)",
"Min, Max", "Skewness"))
t.test(IL6 ~ group.f, data=xudata, paired=TRUE)
B. A larger “gap” is required for statistical significance when n=3 than when n=>10 because the standard error is larger when the n is smaller. The standard error is an estimate of the variability of the sample mean, and it decreases as the n increases. Therefore when the n is small, there is more uncertainty about the mew, and so a larger difference between groups is needed to achieve statistical significance.
# Add path ----------------------------------------------------------------
subfolders = c("etc", "visualize")
for (i in 1:length(subfolders)) {
a = list.files(path = subfolders[i], pattern = "[.]R$", full.names = TRUE)
for (j in 1:length(a)) {
source(a[j])
}
}
# Add path ----------------------------------------------------------------
subfolders = c("etc", "visualize")
# Check and generate a result folder --------------------------------------
results_path = "../../results/figures/R"
# Generate new results folder
dir.create(results_path, recursive = TRUE)
# Clear workspace
rm(list = ls())
# Clear all ---------------------------------------------------------------
# Clear plots
if (!is.null(dev.list())) dev.off()
source("C:/Users/lione/Desktop/GitHub/meta-analysis-for-VEGF-signaling/code/R/compare_VEGF_binding_to_VEGFR1vsVEGFR2vsNRP1.R", echo=TRUE)
source("C:/Users/lione/Desktop/GitHub/meta-analysis-for-VEGF-signaling/code/R/compare_VEGF_binding_to_VEGFR1vsVEGFR2vsNRP1.R", echo=TRUE)
source("C:/Users/lione/Desktop/GitHub/meta-analysis-for-VEGF-signaling/code/R/compare_VEGF_binding_to_VEGFR1vsVEGFR2vsNRP1.R", echo=TRUE)
install.packages(c("weights", "ggnewscale", "colormap"))
source("C:/Users/lione/Desktop/GitHub/meta-analysis-for-VEGF-signaling/code/R/compare_VEGF_binding_to_VEGFR1vsVEGFR2vsNRP1.R", echo=TRUE)
source("C:/Users/lione/Desktop/GitHub/meta-analysis-for-VEGF-signaling/code/R/compare_VEGF_binding_to_VEGFR1vsVEGFR2vsNRP1.R", echo=TRUE)
install.packages(c("metadat", "glmnet", "rstatix"))
source("C:/Users/lione/Desktop/GitHub/meta-analysis-for-VEGF-signaling/code/R/compare_VEGF_binding_to_VEGFR1vsVEGFR2vsNRP1.R", echo=TRUE)
install.packages(c("mitml", "carData"))
a = list.files(path = subfolders[i], pattern = "[.]R$", full.names = TRUE)
a = list.files(path = subfolders[i], pattern = "[.]R$", full.names = TRUE)
source("C:/Users/lione/Desktop/GitHub/meta-analysis-for-VEGF-signaling/code/R/compare_VEGF_binding_to_VEGFR1vsVEGFR2vsNRP1.R", echo=TRUE)
source("C:/Users/lione/Desktop/GitHub/meta-analysis-for-VEGF-signaling/code/R/compare_VEGF_binding_to_VEGFR1vsVEGFR2vsNRP1.R", echo=TRUE)
vegfkonkoff <- as,datframe(read.excel(filename))
vegfkonkoff <- as.datframe(read.excel(filename))
vegfkonkoff <- as.dataframe(read.excel(filename))
vegfkonkoff <- as.data.frame(read.excel(filename))
vegfkonkoff <- as.data.frame(read_excel(filename))
vegfkonkoff <- as.data.frame(read_excel(filename))
View(vegfkonkoff)
source("C:/Users/lione/Desktop/GitHub/meta-analysis-for-VEGF-signaling/code/R/compare_spr_radioligand.R", echo=TRUE)
View(vegfr1_spr)
View(vegfr1_spr)
View(vegfr2_radio)
View(vegfr2_radio)
View(vegfr2_radio)
View(vegfr2_radio)
View(rm_vegfr2_radio)
View(rm_vegfr2_radio)
View(rm_vegfr2_radio)
View(rm_vegfr2_radio)
View(radio_r1_vs_r2)
View(radio_r1_vs_r2)
View(radio_r2_vs_n1)
View(radio_r2_vs_n1)
View(vegfr1_ttest)
View(vegfr1_ttest)
View(vegfr2_radio)
View(vegfr2_radio)
View(nrp1_radio)
